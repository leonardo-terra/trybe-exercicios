const INITIAL_STATE = {
    name:'',
    email:'',
    image:'',
    descriptions:'',
  };

  const user = (state = INITIAL_STATE, action) => {
    switch (action.type){
      default:
        return state;
    }
  }

  export default user;
  